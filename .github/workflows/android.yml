# Code adapted from:
# GitHub Repository: https://github.com/actions/starter-workflows/blob/main/ci/android.yml
# Author: Josh Soref
# License: MIT

name: PR Checks

on:
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build project
        run: ./gradlew build